#include "main.h"

/**
 * _abs - prints absolute values of nums
 *@n: an integer
 *
 * Return: integer.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

}
